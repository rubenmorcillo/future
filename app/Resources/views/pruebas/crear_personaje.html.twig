{% extends 'base.html.twig' %}

{% block content %}
    <h2>Crea a tu comandante</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('homepage') }}"><i class="fas fa-step-backward"></i>Volver al inicio</a>
    <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i> Guardar cambios</button>

    {{ form_end(form) }}

{% endblock %}